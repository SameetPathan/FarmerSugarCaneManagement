onChange={(e) => {
    this.selectionHandle(e.target.value);
    if (e.target.value === "sam") {
      // call your function here
      console.log("Option 'sam' was selected");
    }
  }}
  defaultValue={this.state.fetchedServiceProvider.find(
    (data) => data.abbreviation_endingdate_planyear === "sam"
  )?.abbreviation_endingdate_planyear || "NONE"}
