<Col className="col-1 padding-none center-align text-center" style={{display:"flex"}}>
  <div className={`rollover-icon ${planItem.rolloverCompanyPlanId ? '' : 'hidden'}`}>
    <img
      title="Rollover Plan"
      src="./img/rollover-icon.svg"
      style={{
        width: "20px",
        height: "20px",
        marginRight: "15px",
      }}
    ></img>
  </div>
  <div className={`lock-icon ${planItem.isPlanLock ? '' : 'hidden'}`}>
    <img
      title="Plan Locked"
      src="./img/lock-icon.svg"
      style={{
        width: "20px",
        height: "20px",
        marginRight: "15px",
      }}
    ></img>
  </div>
  <div className={`invoice-icon ${props.EnabledInvoice ? '' : 'hidden'}`}>
    { planItem.invoiceStatus === "INITIATED" || planItem.invoiceStatus === "ERROR" ? 
      <img onClick={() => { props.togglePopover(planItem.companyPlanId, planItem.cpaId) }}
        style={{ cursor: planItem.invoiceStatus === "INITIATED" || planItem.invoiceStatus === "ERROR" ? 'pointer' : 'default', width: "20px", height: "20px"}}
        title={props.getInvoiceIconTip(planItem.invoiceStatus)} src={props.getInvoiceIconColor(planItem.invoiceStatus)} ></img>
      :
      <img title={props.getInvoiceIconTip(planItem.invoiceStatus)} src={props.getInvoiceIconColor(planItem.invoiceStatus)}
        style={{ width: "20px", height: "20px" }}></img>
    }
  </div>
</Col>
